name = "sa1l-static-site"
main = "static-site-worker.js"
compatibility_date = "2025-12-23"

# No bindings needed - this worker only handles HTML partial injection
# Routes configured in Cloudflare dashboard
